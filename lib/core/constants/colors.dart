import 'package:flutter/material.dart';

class AppColors {
  static const Color primaryGreen = Color(0xFF008953); // Brand green
  static const Color primaryNavy = Color(
    0xFF008953,
  ); // Alias kept for compatibility
  static const Color secondaryNavy = Color(
    0xFF006E43,
  ); // Darker green for depth
  static const Color accentCoral = Color(0xFFFFFFFF); // White
  static const Color softWhite = Color(0xFFFFFFFF); // White
  static const Color textMain = Color(0xFFFFFFFF); // White
  static const Color textSecondary = Color(
    0xFFCCE8DA,
  ); // Light green-tinted secondary text
  static const Color accentGold = Color(0xFFFFD700);
}
